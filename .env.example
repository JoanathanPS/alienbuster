# Copy to .env or (recommended) .env.local and fill in your Supabase project values.
# Vite exposes variables prefixed with VITE_ to the browser.

VITE_SUPABASE_URL=""
VITE_SUPABASE_PUBLISHABLE_KEY=""

# Optional: base URL for the local ML API (leave blank to use Vite dev proxy)
# Example: http://127.0.0.1:8000
VITE_API_BASE_URL=""

# Optional (not currently used in code, but handy for scripts/docs)
VITE_SUPABASE_PROJECT_ID=""
