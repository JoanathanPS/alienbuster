# Earth Engine project to initialize with
# This repo's Gradio demo (ver5.py) uses: alien-buster
EARTH_ENGINE_PROJECT="alien-buster"

# Optional: override end date for NDVI queries (Earth Engine expects YYYY-MM-DD)
EARTH_ENGINE_END_DATE=""
